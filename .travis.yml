language: node_js

sudo: required

services:
  - docker

node_js:
  - "8.12"

script:
  - yarn flow
  - yarn lint
  - yarn test --coverage
  - yarn build
  - rm -rf node_modules build coverage
  - docker build . -t ipfs_print_hash
  - docker run ipfs_print_hash
