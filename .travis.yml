language: node_js

sudo: required

services:
  - docker

node_js:
  - "8.12"

script:
  - yarn flow
  - yarn lint
  - yarn test --coverage
  - yarn build
  - git clean -dfx
  - docker run ipfs_print_hash
